struct idAlertCycle_Pain : idAIState
{
    bool hasReentered; // Offset: 0x130

    // if true, finish anim action when dest anim blends in, rather than via blend out
    bool finishOnDestBlendStart; // Offset: 0x131

}; // Size: 0x138
