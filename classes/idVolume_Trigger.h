struct idVolume_Trigger : idVolume
{
    // only trigger this volume once?
    bool triggerOnce; // Offset: 0xC20

    // show a visible trigger
    bool isVisible; // Offset: 0xC21

}; // Size: 0xC28
