struct trickTypeInfoGenToInstantiateTypeDef
{
    // TypeDefs only get instantiated in the project they are used as variables in. This means we need to instantiate TypeDefs in Engine, to have them as classes in engine.(From the perspective of TypeInfo)
    idAtomicHandleT < aliasRefType_t > trick_aliasRef_t; // Offset: 0x0

}; // Size: 0x8
