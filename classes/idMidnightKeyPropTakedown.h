struct idMidnightKeyPropTakedown : idMidnightKeyProperties
{
    // Can we rotate?
    bool canRotate; // Offset: 0x10

    // How fast can we rotate?
    idMidnight::idTakedownRotationSpeed_t rotationSpeed; // Offset: 0x14

}; // Size: 0x18
