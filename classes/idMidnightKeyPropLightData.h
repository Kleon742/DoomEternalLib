struct idMidnightKeyPropLightData : idMidnightKeyProperties
{
    // light data
    idMidnightLightData lightData; // Offset: 0x10

    // tangent in
    idMidnightLightTangentData tangentIn; // Offset: 0x118

    // tangent out
    idMidnightLightTangentData tangentOut; // Offset: 0x138

}; // Size: 0x158
